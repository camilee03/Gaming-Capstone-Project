using Unity.VisualScripting;
using UnityEngine;

public class Tile : MonoBehaviour
{
    // which edge its on (what its parent is)
    // what tiles are next to it?
    // if its a center or edge tile
    // If it has a chair / table spawned on top of it (that might just be their own pools tho)

}
